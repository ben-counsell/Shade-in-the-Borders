{# consider adding folders to better orgnise your templates #}
<html>
    <head>
        <title>{{title}}</title>
        {# can consider adding another block here if you want to add css for only certain templates it would look like this, i've added the other part of the example into admin_edit_stock.jinja #}
        {%block head%}
        {%endblock%}

        <link rel="stylesheet" href="{{url_for('static', filename='style.css')}}">
        <link rel="stylesheet" href="{{url_for('static', filename='navbar.css')}}">
        <link rel="stylesheet" href="{{url_for('static', filename='orders.css')}}">
        <link rel="stylesheet" href="{{url_for('static', filename='new_order.css')}}">
        <link rel="stylesheet" href="{{url_for('static', filename='edit_stock.css')}}">
    </head>
    <body>
        <header>
            <a href="/"><h1>Shade in the Borders</h1></a>
            <nav class="menu">
                <ul class="menu__list r-list">
                    <li class="menu__group"><a href="/admin" class="menu__link r-link text-underlined">Home</a></li>
                    <li class="menu__group"><a href="/view_orders" class="menu__link r-link text-underlined">View orders</a></li>
                    <li class="menu__group"><a href="/edit_stock" class="menu__link r-link text-underlined">Edit stock</a></li>
                </ul>
            </nav>
        </header>
        <main>
            {%block content%}
            {%endblock%}
        </main>
    </body>
    <footer>
        <a href="/">Return to customer view</a>
    </footer>
</html>